import { Routes as RoutesWrapper, Route } from "react-router-dom";

const Routes = () => {
  return (
    <RoutesWrapper>
      <Route path="/" element />
    </RoutesWrapper>
  );
};

export default Routes;
